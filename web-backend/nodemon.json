{
  "watch": ["package.json", "src", ".env"],
  "ext": "js,json",
  "scripts": {
    "prettier": "prettier --check .",
    "start": "node --experimental-specifier-resolution=node src/server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --exec npm start"
  }
}
